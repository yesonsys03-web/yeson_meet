[project]
name = "yeson-meet-sdk"
version = "0.0.0"
description = "yeson-meet Python SDK for internal PyQt5 tools (skeleton)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["yeson_meet_sdk"]
